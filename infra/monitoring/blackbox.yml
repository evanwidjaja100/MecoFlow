modules:
  mecoflow_https_2xx:
    prober: http
    timeout: 8s
    http:
      fail_if_not_ssl: true
      follow_redirects: false
      method: GET
      preferred_ip_protocol: ip4
      tls_config:
        ca_file: /etc/mecoflow/tls/staging-ca.crt
        server_name: mecoflow.localhost
