# Application containers

Application Dockerfiles live with each deployable. Images use Node 24 and non-root runtime users. Compose currently starts local dependencies; a staging-like application profile can be added after the foundation is verified.
