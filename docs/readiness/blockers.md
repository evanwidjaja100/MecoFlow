# Release blockers — Phase 0 record

Each blocker has exactly one primary phase. Downstream revalidation is listed
separately and never changes primary ownership. `OPEN` means the stated
acceptance test has not been satisfied by authoritative evidence.

| ID   | Blocker                                                                                                                                                | Severity | Primary phase | Downstream             | Accountable owner         | Status                | Acceptance test                                                                                                                                       |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ------------- | ---------------------- | ------------------------- | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| B-01 | Five High npm advisories: fast-uri, brace-expansion, js-yaml, nanoid, deepmerge-ts                                                                     | HIGH     | 2             | 13, 14, 17             | `SEC-OWNER`               | `OPEN`                | `pnpm security:audit` exits 0 from a clean deterministic install                                                                                      |
| B-02 | Current pinned Keycloak 26.7.0 diagnostic reports 17 unresolved Critical/High occurrences                                                              | HIGH     | 2             | 14, 17                 | `IDENTITY-OWNER`          | `OPEN`                | Supported digest-pinned Keycloak has zero unresolved Critical/High and authentication/browser smoke passes                                            |
| B-03 | Current scan resolved 5/14 images: Redis passed, four external images have 123 blockers, and nine project images were absent                           | HIGH     | 2             | 14, 17                 | `SEC-OWNER`               | `OPEN`                | `pnpm security:image-scan` freshly scans every policy image by resolved identity and exits 0                                                          |
| B-04 | MinIO is archived and production object migration is not designed or rehearsed                                                                         | HIGH     | 14            | 15–17                  | `PLATFORM-OWNER`          | `OPEN`                | Selected supported store passes provider contract and every object/checksum is reconciled                                                             |
| B-05 | Real production secret, TLS, KMS, network, and external-control evidence is absent                                                                     | HIGH     | 15            | 16, 17                 | `PLATFORM-OWNER`          | `OPEN`                | Candidate-bound production controls pass after the Phase 16 restore                                                                                   |
| B-06 | Real monitoring, on-call/paging, capacity, and degradation evidence is absent                                                                          | HIGH     | 16            | 17                     | `SRE-OWNER`               | `OPEN`                | Candidate-bound `pnpm operations:preflight` passes                                                                                                    |
| B-07 | Human accountable owners and an independent reviewer are unassigned                                                                                    | HIGH     | 0             | all phases             | `BUSINESS-SPONSOR`        | `OPEN`                | Full named roster and approval records satisfy independence for security/data/release-critical changes                                                |
| B-08 | Exact production endpoint values are recorded but lack DNS/change-control evidence, named signers, and candidate-bound approval                        | HIGH     | 0             | 14, 15, 17             | `PLATFORM-OWNER`          | `OPEN`                | Every endpoint row has an exact value, named owner, approval, reviewed SHA, and change-control record                                                 |
| B-09 | Phase 0 design decisions D-01 through D-10 contain exact proposed values but remain unapproved                                                         | HIGH     | 0             | affected later phases  | `BUSINESS-SPONSOR`        | `OPEN`                | Named accountable owners approve all decisions and required independent reviews are recorded                                                          |
| B-10 | RHBK 26.6.5/RHEL EC2 and AWS S3 production models are proposed but not owner-approved or procured                                                      | HIGH     | 0             | 14–17                  | `PROCUREMENT-OWNER`       | `OPEN`                | D-08/D-09 define ADR-consistent supported models, contracts, ownership, procurement, exit strategy, and approvals                                     |
| B-11 | `main` protection requires three strict checks and one approval; no run has all checks passing and only one collaborator exists                        | HIGH     | 0             | every evidence phase   | `REPO-ADMIN`              | `OPEN`                | Pull request #1 receives an independent approval and all three required checks pass; force-push/deletion denial remains enforced                      |
| B-12 | `production` has protected-branch policy and administrator bypass disabled, but no independent required reviewers or least-privilege deployment secret | HIGH     | 0             | 14–18                  | `REL-MANAGER`             | `OPEN`                | Protected `production` environment has independent approval, no administrator bypass, and least-privilege deployment permissions                      |
| B-13 | Historical candidate `6d91208` and current source checkpoint `d345afd` are unapproved/unmerged; runs through `32383852323` are failed diagnostics      | HIGH     | 0             | every phase            | `REL-MANAGER`             | `OPEN`                | Approve and merge the reviewed Phase 0 changes, then reproduce the complete gate from the protected `main` candidate with retained successful outputs |
| B-14 | Standalone `psql` is unavailable, but Docker-backed isolated database execution completed                                                              | LOW      | 0             | Phase 0 local evidence | `IMPLEMENTATION-OPERATOR` | `RESOLVED-DIAGNOSTIC` | Fresh databases applied 22 migrations/seed; integration 29 files/116 tests, authorization 14/53, and E2E 15/15 passed; repeat from committed CI       |
| B-15 | Original untracked readiness-draft bytes were edited before a separate preservation copy existed                                                       | HIGH     | 0             | source attribution     | `REPO-ADMIN`              | `OPEN`                | User approves supersession or provides the originals for byte-level reconciliation                                                                    |
| B-16 | Committed image controls still lack endpoint-backed proof; the identity-error parser correction is verified but images remain unbuilt                  | HIGH     | 0             | 14, 17                 | `REL-MANAGER`             | `OPEN`                | Candidate build exports all nine exact image IDs; fail-closed Compose validation and retained evidence prove deployment uses them without rebuild     |
| B-17 | All five governed labels exist, but candidate-bound authenticated evidence and approval are absent                                                     | HIGH     | 0             | every phase            | `REPO-ADMIN`              | `OPEN`                | Candidate-bound labels API evidence and an authenticated approval prove all five exact governed labels and descriptions exist                         |
| B-18 | Repository is public and user intent is known, but no authenticated candidate-bound owner attestation exists                                           | HIGH     | 0             | every evidence phase   | `REPO-ADMIN`              | `OPEN`                | Authenticated owner approval records the candidate-bound visibility decision and security implications, or visibility is restored                     |

The B-01/B-02/B-03 diagnostics are recorded in `IMPLEMENTATION_STATUS.md` and
`evidence-index.md`. They are not Phase 0 closure evidence because the committed
candidate is not owner-approved and no retained CI run has been accepted.

## B-01 dependency advisory baseline

The 2026-08-11 Phase 0 diagnostic ran
`pnpm audit --audit-level moderate --json` against lockfile SHA-256
`99CA92F59DC8DC6F441CBD300A549AE7990F9DB081B775B4A7610B43A840B1BB`.
On 2026-08-20 the unchanged lockfile was audited again after CI detected
registry drift. It reports exactly five High advisories, no
Critical/Moderate/Low advisories, and 585 total dependency nodes. The new
`deepmerge-ts` advisory and the registry's corrected `nanoid` vulnerable range
are recorded below. This is inventory evidence only: Phase 0 does not authorize
dependency changes or accept any advisory risk.

| Advisory                                                                 | Package           | Installed | Vulnerable range | Patched range | Dependency reach in the audited graph                    | Primary phase / status |
| ------------------------------------------------------------------------ | ----------------- | --------- | ---------------- | ------------- | -------------------------------------------------------- | ---------------------- |
| [GHSA-7p8r-x3mc-p8w7](https://github.com/advisories/GHSA-7p8r-x3mc-p8w7) | `fast-uri`        | `3.1.4`   | `>=3.0.0 <3.1.5` | `>=3.1.5`     | 2 transitive Prisma development-tool paths through `ajv` | Phase 2 / `OPEN`       |
| [GHSA-rgw5-rvv9-x895](https://github.com/advisories/GHSA-rgw5-rvv9-x895) | `brace-expansion` | `5.0.8`   | `>=4.0.0 <5.0.9` | `>=5.0.9`     | 64 transitive lint/tooling paths through `minimatch`     | Phase 2 / `OPEN`       |
| [GHSA-5p4m-2wfm-xmqj](https://github.com/advisories/GHSA-5p4m-2wfm-xmqj) | `js-yaml`         | `4.3.0`   | `>=4.0.0 <4.3.1` | `>=4.3.1`     | 1 API build/tooling path through `@nestjs/swagger`       | Phase 2 / `OPEN`       |
| [GHSA-2v37-7h3g-55p8](https://github.com/advisories/GHSA-2v37-7h3g-55p8) | `nanoid`          | `3.3.16`  | `<3.3.18`        | `>=3.3.18`    | 5 web/test-tool paths through `postcss`                  | Phase 2 / `OPEN`       |
| [GHSA-ggr8-5vv4-36mx](https://github.com/advisories/GHSA-ggr8-5vv4-36mx) | `deepmerge-ts`    | `7.1.5`   | `<8.0.0`         | `>=8.0.0`     | 2 transitive Prisma configuration paths                  | Phase 2 / `OPEN`       |

The exact dependency paths remain available in the raw audit output for the
candidate-bound CI run. Any lockfile change invalidates this baseline and
requires a fresh audit; Phase 2 must remediate or obtain a separately governed
risk decision before a later production-readiness claim.
